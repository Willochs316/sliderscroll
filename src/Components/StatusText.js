const GalleryImageText = ({ title, className }) => {
  return (
    <>
      {/* <div className="txt"> */}
      <p className={className}>{title}</p>
      {/* </div> */}
    </>
  );
};
export default GalleryImageText;
